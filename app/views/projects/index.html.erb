<p id="notice"><%= notice %></p>

<% @projects.each do |project| %>
  <div class="index_row clearfix">
    <div class="todo_list_title"><%= link_to project.title, project %></div>
    <div class="todo_list_subtitle"><%= project.description %></div>
  </div>
<% end %>

<div class="links">
  <%= link_to  "New Project", new_project_path %>
</div>
